// This file is INTENTIONALLY REMOVED from the active build.
//
// It previously provided a cross-platform shim around `mediapipe_genai`
// using prefixed imports (`real.LlmInference`, `real.MPImage`, etc.), but
// those symbols do not exist in mediapipe_genai 0.0.1-dev.3.
//
// The inline stub approach in `gemma_on_device_llm_engine.dart` serves the
// same purpose and compiles cleanly on all platforms.
//
// When the MediaPipe GenAI package stabilises, this file can be restored as
// a conditional-import shim if needed.
